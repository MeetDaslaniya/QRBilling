name: billing_system
description: A clean Flutter billing system starter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  mobile_scanner: ^5.0.0
  supabase_flutter: ^2.0.0
  shared_preferences: ^2.2.2
  get: ^4.6.6
  pdf: ^3.10.7
  path_provider: ^2.1.2
  open_file: ^3.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
